import Std.Option (Some, None)

val x = Option.map_option (fn n => n + 1) (Some 41)

val n = Option.get_or x 0

val _ = println (int_to_string n)

val _ = println (if Option.is_some x then "some" else "none")

val _ = println (if Option.is_none None then "none" else "some")
